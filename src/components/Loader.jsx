import React from "react";
import lloader from '../loader2.gif'
const Loader = () => {
  return (
    <div className="text-center">
      <img src={lloader} alt="loader" />
    </div>
  );
};

export default Loader;
